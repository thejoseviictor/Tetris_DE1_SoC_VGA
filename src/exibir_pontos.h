
#ifndef EXIBIR_PONTOS_H
#define EXIBIR_PONTOS_H

// Declaração da função
int exibir_pontos(int pontos);

#endif

/*

O que colocar no cabeçalho da main.c: #include "exibir_pontos.h"

Onde colocar a função exibir_pontos: Logo após pontuação é atualizada ( exibir_pontos(pontuação); )

Makefile: gcc main.c button.c exibir_pontos.c return_decomposto.c -o tetris2exe -std=c99

*/
